## Packages
react-simple-maps | Interactive SVG maps for the US map component
d3-scale | Color scales for the map visualization
recharts | Time-series visualization for housing trends
clsx | Utility for conditional class names (often used with tailwind-merge)
tailwind-merge | Utility for merging tailwind classes
date-fns | Date formatting for charts and tooltips
lucide-react | Icons (already in base, but confirming usage)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
