You are working inside my existing Replit project using Node.js, Express, TypeScript, and PostgreSQL. Implement an AI Content Engine that uses the OpenAI API (server-side only, via process.env.OPENAI_API_KEY) to (1) generate daily LinkedIn/Facebook post ideas based on recent U.S. housing and real estate news from the last 7 days, and (2) generate social posts and optional article outlines when an idea is selected. Add the openai SDK, keep all API calls on the backend, and document required env vars in .env.example (OPENAI_API_KEY, SITE_BASE_URL). Create two Postgres tables with migrations: ai_daily_ideas (id, run_date unique, payload jsonb, created_at) and ai_generated_content (id, run_date, idea_id, topic, channels jsonb, links jsonb, created_at, unique on run_date + idea_id). Add a GET /api/ai/ideas/today endpoint that checks today’s row in ai_daily_ideas; if missing, calls OpenAI to return 5–10 ideas (id, hook, angle, why_now, suggested_keywords, housing_intel_links with {label, url_path}), saves the JSON payload, and returns it; otherwise return the cached row. Add a POST /api/ai/content/generate endpoint that accepts { ideaId, ideaText?, urlPaths: string[], utm? }, builds full URLs using SITE_BASE_URL and UTM params, checks ai_generated_content for today + ideaId, returns cached content if present, otherwise calls OpenAI to generate a suggested_title, a data-driven LinkedIn post (120–220 words, not salesy, no alarmist language), a slightly shorter Facebook post (80–160 words), an optional article outline (5–8 bullets), and a short CTA, then saves and returns the result along with the generated links. Use zod for request validation, strict TypeScript, centralized helpers (lib/openai.ts, lib/prompts.ts, lib/utm.ts), and proper error handling. Prefer OpenAI’s built-in web/news search for “recent news”; if unavailable, fall back to lightweight RSS ingestion from reputable housing outlets and cache headlines per day. Optionally add a daily scheduler (or admin refresh endpoint) to pre-generate ideas each morning. Ensure no OpenAI key leakage, cache results to minimize cost, and provide example curl commands and a short README section explaining the AI Content Engine.